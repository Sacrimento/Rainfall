main(argv, env):
{
	if(atoi(argv[1]) == 423)
	{
		fwrite("No !\n", 1, 5, edx);
		return;
	}
	char str = strdup("/bin/sh");
	setresgid(getgid(), geteuid());
	setresuid(getgid(), geteuid());
	execv(str, env);
	return 0;
}
