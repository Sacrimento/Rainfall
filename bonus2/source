
greetuser(int32_t ptr):
{
	s1[72] = "Hello";

	if (obj.language == 1)
		s1 = "Hyvää päivää";
	else if (obj.language == 2)
		s1 = "Goedemiddag!";

	strcat(s1, ptr);
	puts(s1);
}

main:
{
	stack:160
	if (argc != 3)
		return 1;
	ecx =13;
	strncpy(buf, argv[1], 40);
	strncpy(buf, argv[2], 32);
	truc = getenv("LANG")
	if (!)
		-> main + 239
	if ( memcmp(truc, "fi", 2) )
	else if ( memcmp(truc, "nl", 2) )
}
