greetuser:
{
	stack:0x58
	if (eax == 1)
	else if (eax == 2)

	strcat;
	puts;

}

main:
{
	stack:160
	if (argc != 3)
		return 1;
	ecx =13;
	strncpy(buf, argv[1], 40);
	strncpy(buf, argv[2], 32);
	truc = getenv("LANG")
	if (!)
		-> main + 239
	if ( memcmp(truc, "fi", 2) )
	else if ( memcmp(truc, "nl", 2) )
}
