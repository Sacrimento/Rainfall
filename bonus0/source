p(buf, str):
puts(str);
int elem1 = 4096
read(0, buff, elem1);
ptr = strchr(buff, "\n");

/* argv[2] ebp + 0x8 */

strncpy(argv[2], ptr, 20);

pp:
str1 = p(buff, " - ");
str2 = p(buff, " - ");

[...]

strcpy(buffer, str1);

[...]

strcat(buffer, str2);


main:
char *str;
str = pp();
puts(str);
