p(buf, str):
{
	buffer[4120];
	puts(str);
	read(0, buff, 4096);
	ptr = strchr(buff, "\n");
	return strncpy(argv[2], ptr, 20);
}

pp:
{
	buf1;
	buf2;

	str1 = p(buf1, " - ");
	str2 = p(buf2, " - ");

	[...]

		strcpy(buffer, str1);

	str2[]
	[...]

		return strcat(buffer, str2);
}


main:
char str[64];
pp(str);
puts(str);
