p():
{
	char string[76];

	fflush(stdout);

	gets(string)
	if (*(&string + sizeof(string) + 4) & 0xb0000000)
	{
		puts(string);
		strdup(string);
		return ;
	}
	printf("(%p)\n", *(&string + sizeof(string) + 4);
	exit(1);
}

main:
{
	p()
}
