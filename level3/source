v(void):
{
	char *str;

	printf(fgets(str, 512, stdin));

	printf(argv[1]);
	if (obj.m != 64) //obj.m -> 0x804988c:4
		return;
	fwrite("Wait what?!\n", 1, 12, stdout);
	system("/bin/sh");
}

main:
{
	v()
}
